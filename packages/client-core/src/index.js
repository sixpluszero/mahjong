/**
 * 中文：跨端共享客户端运行时入口（当前为脚手架）。
 * 后续将承载：WS 生命周期、会话恢复、房间/牌局状态同步。
 * EN: Shared cross-platform client runtime entry (scaffold for now).
 * Future: WS lifecycle, session resume, room/game state synchronization.
 */

export function createClientCore() {
  return {
    version: '0.1.0-scaffold',
    status: 'todo'
  };
}
