# Mahjong Progress Log

## 开发任务看板（持续更新）

### 已完成
- [x] 冻结规则规格 v1（血战到底、换三张、定缺、点炮、抢杠胡）
- [x] 初始化 monorepo（`packages/shared`、`packages/server`、`packages/web`）
- [x] 实现 shared 规则与状态机核心（发牌/换三张/定缺/摸打/响应优先级）
- [x] 完成结算主链路（点炮/自摸/明杠/暗杠/补杠/抢杠胡）并通过自动化测试
- [x] 实现 WebSocket 好友房闭环 + 网页最小可玩客户端
- [x] 实现再来一局（`request_rematch`）和房间状态广播
- [x] 实现机器人功能（手动添加、自动准备、自动对局、自动同意续局）
- [x] 修复跨局积分累计：新增 `totalScore` 与 `roundHistory`，前端展示总分榜和历史战绩
- [x] 实现断线重连与会话恢复（`seat_assigned` + `resume_room` + 自动重连）
- [x] 实现掉线托管（真人掉线后自动托管，重连后恢复手动控制）
- [x] 结算中心增强：新增分数走势视图，保留番型解释与历史战绩
- [x] 机器人节奏优化：房间有真人在线时，机器人/托管动作延迟 1 秒
- [x] 无真人在线房间保活策略：设置 30 分钟自动关闭倒计时
- [x] 无真人在线时结算后不自动续局，默认等待真人回房
- [x] 房间局数上限：默认 8 局，完赛后锁定并展示最终统计榜

### 进行中
- [ ] 血战到底流局收口扩展：查叫/罚分（作为可配置房规）

### 待办
- [ ] 结算回合汇总输出（每手明细 + 本局总分）给前端直接渲染
- [ ] 房间内“复制房间号 / 邀请链接 / 错误提示面板”增强
- [ ] 断线重连后的重入房与回放日志能力

Original prompt: 我希望在这个麻将游戏中加入机器人功能。当人类玩家在网页端创建房间或者加入房间后，如果还有空位，将允许手动点击按钮添加机器人，并用机器人+随机名字作为玩家id。机器人将自动准备并参与游戏，每局结束后必自动同意加入下一句。目前的功能是写一个最基本的规则，在能出牌的时候出最优的牌，能杠的时候优先杠，能碰的时候碰，能胡的时候必选择胡，这个能做到吗？你做完之后可以本地测试：你可以直接创建四个机器人的房间并测试能否完成一局。

## 2026-02-28

### 已完成
- 冻结规则规格 v1（四川血战到底、换三张、定缺、点炮、抢杠胡）。
- 初始化 monorepo：`packages/shared`、`packages/server`、`packages/web`。
- 实现 `shared` 规则核心：
  - 牌模型与 108 张牌堆
  - 胡牌判定（标准和型、七对/龙七对）
  - 番型计算与封顶
  - 定缺强约束（出牌/胡牌校验）
- 实现 `shared` 状态机核心：
  - 发牌、换三张、定缺
  - 出牌与响应窗口
  - 响应优先级：胡 > 杠 > 碰
- 建立 `server` WebSocket 好友房最小闭环：
  - `hello` / 建房 / 入房 / ready
  - 开局与对局状态广播
  - 出牌与响应动作接入状态机
- 增加协议文档与调试机器人客户端。
- 自动化测试通过（当前 18/18）。
- 结算引擎增量（shared/game）：
  - 点炮胡牌记账（含一炮多响，逐家结算）
  - 明杠杠分记账（点杠者支付）
  - 自摸结算（向所有未胡玩家收分）
  - 暗杠结算（向所有未胡玩家收分）
  - 补杠流程与抢杠胡响应窗口（抢杠胡成立时补杠不生效）
  - 杠上花/杠上炮计番上下文（通过 `lastDraw` 与出牌上下文传入）
  - 结算事件日志 `settlementEvents`
  - 终局原因字段 `settlementReason`（如 `all_but_one_hu`、`wall_exhausted`）
- 自动化测试更新为 23/23（新增自摸/暗杠/补杠抢杠胡用例）。
- `server` 协议已接入新动作消息：
  - `self_hu`
  - `an_gang`
  - `bu_gang`
  - 并完成服务端处理器到 shared 状态机的接线。
- 新增网页端最小可玩客户端（中文界面）：
  - 支持创建房间、加入房间、准备开局
  - 支持手牌点击出牌、响应胡/杠/碰/过
  - 支持自摸、暗杠、补杠
  - 支持查看副露、分数和结算事件
- `server` 已托管静态网页资源，直接访问 `http://localhost:8787` 可使用。
- 网页端稳定性修复：
  - 所有按钮显式使用 `type=\"button\"`，避免误触发表单提交刷新
  - 增加全局错误捕获与状态提示，前端异常可直接在页面看到
  - 对局信息渲染增加空值保护，避免脚本中断
- 增加四客户端随机压测脚本（`server/scripts/stress-random.mjs`）：
  - 自动启动服务端
  - 自动连接 4 个 WebSocket 客户端
  - 按规则随机执行换三张/定缺/出牌/响应/杠/自摸
  - 支持多局统计与错误汇总
- 服务端并发竞态容错优化：
  - 对 `NOT_YOUR_TURN`、`REACTIONS_PENDING`、`TILE_NOT_IN_HAND`、`INVALID_GAME_PHASE*` 等网络竞态类动作改为容错同步，不再记为服务端异常
- 压测结果：`10` 局随机对局全部通过，`wsMessageErrors=0`。
- 修复“副露后自摸胡误判”问题：
  - 胡牌评估纳入副露结构（碰/杠），不再只按纯手牌判断
  - 新增回归测试：副露场景下可自摸胡
- 网页对局信息增强：
  - 新增弃牌堆展示（最近在前，标记是否被响应）
  - 玩家列表展示每位玩家的副露、缺门与已胡状态
  - 暗杠只显示“暗杠”，不显示具体牌
  - 定缺/响应动作中文化（万/条/筒、胡/杠/碰/过）
  - 新摸牌提示：固定最右显示 + “新”角标 + 高亮闪烁
- 新增根命令 `npm run stress`，可直接执行 4 客户端随机压测。
- 结算可视化与续局能力增强：
  - 结算事件从 JSON 改为可读时间线文案（胡/杠明细）
  - `settlement` 阶段新增“再来一局”按钮
  - 新增 `request_rematch` 协议，所有在房玩家确认后自动开启下一局
  - `room_state` 新增 `phase` 与 `rematchReadySeats` 状态广播
- 联机可用性增强（多设备场景）：
  - 昵称支持一键随机生成
  - 随机昵称改为加密随机源并避免连续重复
  - 新增“活跃房间”列表（显示当前服务器中 >=1 人房间）
  - 支持在房间列表中一键加入（满房自动禁用）
  - 新增 `list_rooms` 协议与 `rooms_list` 响应
  - 新增连接诊断面板（wsUrl/readyState/close code/error）与一键重连
- 服务端启动日志显示局域网可访问地址，便于 iPad 接入
- 新增机器人功能（server/web）：
  - 新协议 `add_bot`，房间内有空位时可手动添加机器人
  - 机器人玩家命名 `机器人+随机昵称`，并在 `room_state.players[].isBot` 标识身份
  - 机器人在开局前自动 `ready=true`
  - 机器人自动参与 `exchange/lack/play` 阶段
  - 机器人动作优先级：
    - 响应阶段：`hu > gang > peng > pass`
    - 自己回合：优先 `self_hu`，再 `bu_gang/an_gang`，最后按启发式出牌
  - 机器人自动在 `settlement` 阶段确认再来一局
  - 前端新增“添加机器人”按钮，房间未开局且未满员时可用，玩家列表显示机器人标记
- 本地联调验证：
  - `npm test` 通过（24/24）
  - 脚本验证“1人建房 + 3机器人 + 人类自动打牌”可进入结算：`reason=all_but_one_hu`
- 计分修复与战绩增强：
  - 修复“再来一局总分清零”问题：新增房间级累计总分 `players[].totalScore`
  - 新增房间级 `roundHistory`，记录每局每位玩家 `delta` 与 `totalScore`
  - 新增前端“总分榜 / 历史战绩（最近10局）”展示
  - 本地两局联调验证：`roundHistory` 长度递增且总分累计正确

### 当前状态
- 已有“可联机跑通流程”的后端基础版本（偏引擎与协议验证）。
- 网页端已接入，可多开 4 个页面模拟四人对局。
- 结算主链路已覆盖：点炮/自摸/明杠/暗杠/补杠/抢杠胡。
- `server` 已开放新动作消息入口，客户端可以直接发起自摸/暗杠/补杠意图。

### 下阶段（优先顺序）
1. 补齐血战到底终局与流局收口（含查叫/罚分的可配置钩子）。
2. 增加结算回合汇总输出（每手明细 + 本局总分）给前端直接渲染。
3. 网页端补充“房间内复制房间号、一键邀请链接、错误提示面板”。
4. 开发网页端房间页与对局页，接 WebSocket 协议。
5. 增加断线重连与回放日志。

## 2026-03-02

### E2E 测试接入（进行中）
- 新增最小 Playwright E2E 骨架：`playwright.config.js` + `e2e/invite-link-room-join.spec.js`。
- 前端增加 E2E 稳定选择器（`data-testid`）用于昵称、建房、入房、房间信息、玩家列表等关键节点。
- 在 `copyInviteBtn` 上新增 `data-invite-link`（由渲染阶段注入），规避剪贴板权限导致的测试不稳定。
- 根脚本新增：`npm run test:e2e` / `npm run test:e2e:headed`。
- README 已补充 E2E 运行步骤。
- 风险：当前环境离线，`npm install -D @playwright/test` 失败（ENOTFOUND registry.npmjs.org），待联网后安装并跑通 E2E。

### TODO
- 联网后执行：`npm install`、`npx playwright install chromium`、`npm run test:e2e`。
- 若 CI 环境无浏览器缓存，确保增加 Playwright browser install 步骤。

## 2026-03-03

### macOS 客户端（大规模 UI/交互迭代）
- 完成 mac 客户端牌桌重构（`apps/mac/src/App.tsx`）：
  - 四方位座位信息（上/左/右/下）+ 中央局况 HUD + 弃牌河 + 手牌区。
  - 实时动作条（胡/杠/碰/过）与倒计时展示，支持 15 秒占位倒计时与颜色分级（绿/黄/红）。
  - 当前回合玩家高亮（座位发光 + 标记）。
- 川麻术语统一：移除“副露”等非川麻措辞，界面与文档统一为“碰/杠/定缺”等表达。
- 手牌与牌桌可读性增强：
  - 非可操作态手牌降饱和。
  - 最近出牌中央信息提示（玩家名 + 中文牌名）。
  - 弃牌河改固定网格，减少跳动。
  - 反应窗口出现时高亮目标弃牌。
- 碰/杠展示与一致性修复：
  - 碰杠改为牌组视觉组件，不再文本摘要。
  - 暗杠仅本人可见（前端强约束）。
  - 修复碰/杠后弃牌堆不移除已被拿走牌的问题（claimed 过滤）。
  - 修复自家杠牌组同步与杠牌张数渲染鲁棒性问题。
- 结算体验增强：
  - 新增可关闭的“结算弹窗”遮罩层（显示本局分变化 + 总分）。
  - 本局第一名（金色高亮）。
  - “本局亮牌”支持牌块视觉显示，且包含手牌 + 碰杠牌（同一行，间距分隔）。
- 分数与事件流：
  - 左侧新增“得分信息流”面板（倒序、加减分着色、番型高亮）。
  - 对齐 Web 端数据源：读取 `settlementEvents` + `roundHistory` 生成信息流与本局分。
  - 兜底：当事件缺失时，按总分变化推导分差与信息流（并去重）。
- 亮牌数据链路联调：
  - server 结算阶段在 `game_state.state` 注入 `revealedHands`。
  - mac runtime 读取并保留 `revealedHands`，修复被 `room_state` 覆盖清空的问题。
  - 增加亮牌数据条目提示用于现场诊断。
- 布局与信息架构调整：
  - 房间管理相关按钮迁移至牌桌左侧独立 panel。
  - 局况卡位置与宽度多轮调优（中置、单行压缩、可读性增强）。
  - 操作/倒计时 panel 移至手牌 panel 下方。

### 文档与清单
- 更新 `apps/mac/TEST-CHECKLIST.md` 为川麻整局 smoke 清单。
- 更新 `apps/mac/ROADMAP.md` 术语口径（peng/gang）。

### 当前已知与后续
- 若“亮牌数据条目”仍为 0，优先核验服务端实际运行进程是否为最新代码版本与端口实例。
- 后续可继续把“本局亮牌”从单行列表升级为更紧凑的结算牌阵视图（按玩家分组卡片）。

### 2026-03-03（macOS 界面商业化改造 v1）
- 在 `apps/mac/src/App.tsx` 完成第一轮视觉系统收敛：
  - 新增全局 UI token（颜色、边框、分层）常量 `THEME`，统一页面/面板/按钮/桌面区域色彩。
  - 左侧房间管理重构为三段式结构：会话设置、房间操作、得分信息流，降低信息拥挤度。
  - 得分信息流默认仅展示最近 6 条，增加“展开更多/收起”控制，减少常驻噪音。
  - `Btn` 组件升级为 `primary/secondary/danger/ghost` 四种变体，统一高度、圆角、字号和禁用态。
  - 牌桌区域与玩家卡片降重边框，改为轻边框+层次背景；当前回合高亮从金色描边改为品牌蓝柔和发光。
  - 结算弹窗与动作条配色从偏紫改为与主视觉一致的深蓝系，提高一致性与商业感。
- 验证：执行 `npm test`（root -> `@mahjong/shared`）通过，25/25。

### UI 后续 TODO（建议下一轮）
- 为按钮/卡片补充按下态与过渡动画（React Native Pressable state + 120~180ms 动画）。
- 将“房间管理”中的地址/昵称/房间输入抽成复用组件，避免 `App.tsx` 继续膨胀。
- 新增浅色主题与系统主题跟随（优先 macOS 系统主题）。

### 2026-03-03（macOS 界面商业化改造 v2）
- 第二轮 UI 工程化改造完成（`apps/mac/src/App.tsx`）：
  - 主题系统升级为动态主题：新增 `ThemeTokens` + `DARK_THEME/LIGHT_THEME`，使用 `useColorScheme()` 自动跟随系统浅色/深色。
  - 样式生成改为 `createStyles(theme)`，把颜色令牌从静态常量改为主题驱动，避免后续手工重复改色。
  - 左栏组件化：新增 `SidebarSection` 与 `InputField` 复用组件，统一输入框 placeholder/边框/间距规范。
  - 按钮交互态：`Btn` 改为局部组件，增加 pressed 态（轻微位移 + 透明度变化），并保留主/次/危险/幽灵四级语义。
  - 牌块交互态：手牌 `Tile` 新增 pressed 态反馈，提升操作确认感。
  - 视觉一致性增强：toast、河道、弹窗遮罩、次级文本等颜色全面接入主题 token。
- 验证：执行 `npm test` 通过（25/25）。

### UI 后续建议（v3）
- 将中央信息 HUD 与 SeatPanel 再拆为独立文件（`components/`），进一步降低 `App.tsx` 复杂度。
- 加入轻量入场动画（例如回合切换时当前座位 1 次淡入发光）。
- 增加 macOS 系统字体权重映射（Title2/Body/Caption）并统一文本层级常量。

### 2026-03-03（macOS 桌布显示优化）
- 按反馈优化中间牌桌显示面积（`apps/mac/src/App.tsx`）：
  - `tableSurface` 增加 `minHeight: 680`，并加大上下左右内边距，使主舞台在大窗口下不显得局促。
  - `centerHud` 宽度从 90% 提升到 94%，最大宽度提升到 900，顶部信息条更贴合桌面宽度。
  - `riversWrap` 宽度从 62% 提升到 74%，并增加 `minHeight: 300`，中央弃牌/牌阵区域视觉占比明显提升。
  - `bottomSeatWrap` 间距上调，底部玩家区与中区分隔更稳定。
